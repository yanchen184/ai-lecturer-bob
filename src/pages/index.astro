---
/**
 * / — 形象首頁（Hero → 關於 → 技能棧 → 課程 → 最新文章 → 聯繫）
 * 保留 Neub 風格底但移除 /// // 裝飾前綴。
 */
import RootLayout from '../layouts/RootLayout.astro';
import NavbarNeub from '../components/NavbarNeub.astro';
import FooterNeub from '../components/FooterNeub.astro';
// import Testimonials from '../sections/Testimonials.astro';
// 暫時隱藏：等累積真實學員 / 客戶具名回饋再放回（避免樣板回饋反而傷信任）
import MessageBoard from '../components/MessageBoard.tsx';
import CharacterCompanion from '../components/CharacterCompanion.tsx';
import { getAllPublishedPosts } from '../lib/firestore';
import { withBase } from '../lib/url';

const posts = await getAllPublishedPosts();
const latest = posts.slice(0, 3);
const formatDate = (d: string) => d.replaceAll('-', '/');

const stats = [
  { number: '6 repo', label: 'AMI 醫療平台微服務' },
  { number: '10-50', label: '場企業內訓' },
  { number: 'Java + AI', label: '雙棧背景' },
  { number: 'AZ-900', label: 'Azure 認證' },
];

// Portfolio — 真的寫過、上線過的東西，給「Path 03 觀察期」讀者直接看 GitHub
const portfolio = [
  {
    label: 'flagship',
    title: 'AMI 照護平台',
    subtitle: '6-repo 微服務 · 醫療領域',
    body: '醫師工作台 + 民眾端 + AI 服務（OCR + 去敏 + Hermes Multi-Agent + RAG）+ HIS 介接層。Spring Boot 4.0.3 + Python + React + LINE LIFF 全棧落地。',
    techs: ['Spring Boot 4', 'Python', 'React + TS', 'Multi-Agent', 'RAG', 'LINE LIFF'],
    link: 'https://github.com/yanchen184?tab=repositories&q=ami',
    color: '#ff6ec7',
  },
  {
    label: 'gov',
    title: '自然人憑證模擬讀卡機',
    subtitle: '政府案輔助工具',
    body: '為自然人憑證身份驗證流程開發的模擬讀卡機，協助前後端在沒有實體讀卡機的情況下完成整合測試與 demo。',
    techs: ['TypeScript', 'PKCS#11 模擬', '政府身份驗證'],
    link: 'https://github.com/yanchen184/citizen-cert-mock-reader',
    color: '#7cc0ff',
  },
  {
    label: 'teaching',
    title: 'AI 概論初級班 + Docker / K8s 教材',
    subtitle: '職訓局 / 企業內訓教材',
    body: '從 AI 概論入門簡報、Docker 14 小時完整講稿、Kubernetes 實作 lab 到 Java 高併發完全指南——所有教材都是上過課、debug 過、迭代過的版本。',
    techs: ['AI 概論', 'Docker', 'Kubernetes', 'Java 高併發'],
    link: 'https://github.com/yanchen184?tab=repositories&q=course+OR+slides+OR+guide',
    color: '#ffaa00',
  },
  {
    label: 'oss',
    title: 'wez-spring-boot-starters',
    subtitle: '公司共用工程基建',
    body: '統一日誌、統一回應、JPA 通用模組、安全模組——把跨專案重複的 boilerplate 抽成 starter。團隊接新專案少寫一個禮拜。',
    techs: ['Spring Boot starter', 'JPA', '統一日誌', '安全模組'],
    link: 'https://github.com/yanchen184/wez-spring-boot-starters',
    color: '#00ffd1',
  },
];

const productionCases = [
  {
    domain: '醫療領域',
    title: '決策輔助系統的反饋學習機制',
    body: 'AI 模型隨臨床實際使用持續修正的閉環設計——讓模型不是上線就停在那，而是真的越用越準。',
    fitFor: '想做模型持續優化、避免 AI 上線後就靜止的團隊',
    color: '#ff6ec7',
  },
  {
    domain: '企業語音辨識',
    title: '用聯發科 Breeze-ASR-25 落地',
    body: '協助企業導入繁體中文語音辨識（基於聯發科開源 ASR 模型），含部署、領域術語調教、品質監控。本土模型也能用得上。',
    fitFor: '客服中心、醫療逐字稿、會議記錄、客訴分析',
    color: '#ffaa00',
  },
  {
    domain: '企業內部',
    title: '會議記錄 RAG 檢索',
    body: '跨部門會議檔的向量檢索與語意問答，含 ACL 權限切割——不同角色看到不同會議，不靠 prompt 攔截，靠 retrieval 層先過濾。',
    fitFor: '知識管理、企業內部問答、跨部門資料檢索',
    color: '#7cc0ff',
  },
];

const faqs: { q: string; a: string }[] = [
  {
    q: '收費怎麼算？網站上為什麼看不到報價？',
    a: '我選擇「先聊需求再報價」，不放固定價格表。原因很直接：我想用「你們的需求對不對」來篩第一輪，不是用「價格能不能負擔」來篩——這兩件事篩出的客戶很不一樣。聊 30 分鐘了解你們團隊規模、對象、想做到什麼程度，價格我會直接告訴你，不繞圈子、不會有「先聊聊再追加」的情況。',
  },
  {
    q: '客製化流程是怎麼運作的？',
    a: '收到你的需求後我會回一份「課前訪談題庫」（你們團隊現在用什麼工具、卡在哪、想學完做到什麼），約一次 30-60 分鐘對焦會議，之後產出客製大綱給你內部簽核。簽核通過後我才開始備課、寫範例 repo——所以你拿到的東西會是針對你們團隊的，不是通用講義。',
  },
  {
    q: '可以開公司發票嗎？月結可以嗎？',
    a: '可以。我有公司行號，能開二聯 / 三聯發票，月結 30 / 60 也都接受。確認需求後我會發報價單，你內部簽核完回傳，課後 7 個工作日內開立發票。',
  },
  {
    q: '結訓後團隊還能繼續問問題嗎？',
    a: '可以。基本款：結訓後一個月內 email / Slack 群組 Q&A 不額外收費，避免學員回去卡住就放棄。延伸方案：雙週 review 顧問合作——做完一場內訓後最常被選的後續方案，把 AI 工具真的長到你們團隊的工作流裡。',
  },
];

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: {
      '@type': 'Answer',
      text: f.a,
    },
  })),
};

const skills = [
  { group: 'AI', label: 'ai', color: '#ffaa00', items: ['LLM 應用', 'RAG', 'Agent', '語音辨識', 'Prompt 設計'] },
  { group: '後端', label: 'backend', color: 'var(--color-neub-yellow)', items: ['Spring Boot 4', 'Java 21', 'Maven', 'JPA / Hibernate'] },
  { group: '資料', label: 'data', color: '#00ffd1', items: ['MySQL', 'MSSQL', 'Redis', 'RabbitMQ'] },
  { group: '前端', label: 'frontend', color: '#ff6ec7', items: ['React 19', 'TypeScript', 'Vite', 'Tailwind CSS'] },
  { group: '雲端', label: 'cloud', color: '#7cc0ff', items: ['Azure (AZ-900)', 'Docker', 'GitHub Actions', 'Firebase'] },
];

const courses = [
  {
    persona: 'HR / L&D / 學習發展窗口',
    badge: '01',
    title: 'AI 培訓菜單 · 工程師教工程師',
    problem: '老闆說要做 AI 培訓，但你怕找錯講師被工程同事嫌。',
    deliver: [
      '半天 / 一天 / 兩天三種規格，內容客製不是統一講義',
      '給你一份大綱、上課流程、Q&A 預演，可以直接上呈簽核',
      '結訓後給學員一份能帶回工位繼續用的 cheat sheet + 範例 repo',
    ],
    duration: '半天 ～ 兩天',
    tags: ['AI 內訓', 'LLM', 'RAG', '客製大綱'],
  },
  {
    persona: 'CTO / 技術主管 / 工程經理',
    badge: '02',
    title: 'AI 導入顧問 + 內訓套餐',
    problem: '你想推團隊用 AI，但團隊抗拒、不知從哪下手、怕做白工。',
    deliver: [
      '先做場景盤點：哪些痛點適合 AI、哪些不適合，講白話',
      '給可落地的技術選型 + 預算範圍 + 風險清單',
      '內訓 + 雙週 review，讓團隊邊做邊學、不是一場性消費',
    ],
    duration: '單次顧問 ／ 季度合作',
    tags: ['AI 導入', '架構顧問', '場景盤點', 'Workshop'],
  },
  {
    persona: '業務 / PM / 產品主管',
    badge: '03',
    title: '半天 AI 場景 Workshop',
    problem: '你想搞清楚「AI 對我們業務能做什麼、不能做什麼」，又怕被技術名詞繞暈。',
    deliver: [
      '半天 workshop，講白話、不丟英文縮寫',
      '帶著你跟團隊一起列 10-20 個業務場景，現場篩可行性',
      '結束後給一份「下一步該做什麼」的優先級清單',
    ],
    duration: '半天 workshop',
    tags: ['AI 場景盤點', 'PM 友善', '優先級清單'],
  },
];
---

<RootLayout
  title="陳彥彤 YC — 把 AI 真的接到公司系統的 AI 企業內訓講師"
  description="工程師教工程師的 AI 企業內訓與導入顧問。Java 後端 8 年、AI 工程師 2 年。AMI 醫療平台 6-repo 全棧落地（Hermes Multi-Agent + RAG + OCR）、職訓局 / 企業內訓講師、政府案系統開發經驗。HR、技術主管、PM 三種對應方案。"
  canonical="/"
  jsonLd={faqJsonLd}
>
  <NavbarNeub />

  <main class="font-sans text-[#0a0a0a]">
    <!-- Hero -->
    <section data-companion-section="hero" class="px-6 md:px-10 py-20 md:py-28 border-b-[3px] border-black">
      <div class="max-w-7xl mx-auto grid gap-12 lg:grid-cols-[1.6fr_1fr] lg:items-center">
        <div>
        <div class="text-xs uppercase tracking-widest opacity-60 mb-4 font-mono">
          ai enterprise training · ai consulting · taiwan
        </div>
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-black leading-[1.1] md:leading-[1.05] tracking-tight">
          <span class="block">想讓你的團隊</span>
          <span class="inline-block mt-2 bg-[var(--color-neub-yellow)] px-2 md:px-3 box-decoration-clone">
            用 AI 把日常做得更快？
          </span>
        </h1>

        <p class="mt-8 text-lg md:text-xl max-w-3xl leading-relaxed opacity-80">
          我是<strong>陳彥彤 YC</strong>。教 <strong>Claude Code、AI Agent、AI 工作流</strong>——
          課程內容就是我自己每天在 production 用的同一套工具。<br class="hidden md:inline" />
          學員下課就能照著做，下週就能在自己工位繼續用。
        </p>

        <!-- 兩條業務線一目了然 -->
        <div class="mt-8 grid gap-2 max-w-2xl text-sm md:text-base font-mono">
          <div class="flex items-baseline gap-3 leading-relaxed">
            <span class="opacity-50 shrink-0">▎</span>
            <span><strong class="bg-[var(--color-neub-yellow)] px-1">AI 企業內訓主力：</strong>Claude Code、AI Agent、工作效率提升</span>
          </div>
          <div class="flex items-baseline gap-3 leading-relaxed">
            <span class="opacity-50 shrink-0">▎</span>
            <span><strong class="bg-[var(--color-neub-yellow)] px-1">AI 顧問實作：</strong>醫療決策輔助、Breeze-ASR-25 語音辨識、會議 RAG</span>
          </div>
        </div>

        <div class="mt-10 flex flex-wrap items-center gap-3">
          <a
            href="#contact"
            class="px-6 py-3 bg-[var(--color-neub-yellow)] text-black text-sm uppercase tracking-widest border-2 border-black hover:bg-black hover:text-[var(--color-neub-yellow)] transition-colors font-mono font-black"
            style="box-shadow: 4px 4px 0 #0a0a0a;"
          >
            聊聊你的需求 →
          </a>
          <a
            href="#courses"
            class="text-sm uppercase tracking-widest font-mono underline decoration-black/30 decoration-2 underline-offset-4 hover:bg-[var(--color-neub-yellow)] px-1 transition-colors"
          >
            看內訓方案
          </a>
        </div>

        <!-- Stats -->
        <div class="mt-16 grid grid-cols-2 md:grid-cols-4 gap-4">
          {
            stats.map((s, i) => (
              <div
                class:list={[
                  'border-2 border-black px-4 py-5 md:p-6 transition-transform hover:-translate-y-0.5',
                  i === 0 ? 'bg-[var(--color-neub-yellow)]' : 'bg-white',
                ]}
                style="box-shadow: 4px 4px 0 #0a0a0a;"
              >
                <div class="text-2xl md:text-4xl font-black tracking-tight leading-none">
                  {s.number}
                </div>
                <div class="text-xs md:text-sm opacity-70 mt-2 font-mono uppercase tracking-wider">
                  {s.label}
                </div>
              </div>
            ))
          }
        </div>
        </div>

        <!-- Hero 形象圖 -->
        <div class="hidden lg:block">
          <div
            class="relative border-2 border-black bg-[var(--color-neub-yellow)] p-3"
            style="box-shadow: 8px 8px 0 #0a0a0a;"
          >
            <img
              src={withBase('/images/character/body_01.png')}
              alt="陳彥彤 YC 形象插畫"
              width="1024"
              height="1536"
              class="block w-full h-auto bg-white border border-black"
              loading="eager"
              decoding="async"
            />
            <div class="absolute -bottom-3 -right-3 bg-black text-white text-[10px] font-mono uppercase tracking-widest px-2 py-1 border-2 border-black">
              yc · 工程師教工程師
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 差異化 band：一個句子定位 -->
    <section data-companion-section="band" class="relative overflow-hidden border-b-[3px] border-black bg-black text-white">
      <div
        aria-hidden="true"
        class="pointer-events-none absolute inset-0 opacity-[0.08]"
        style="background-image: linear-gradient(#fff 1px, transparent 1px), linear-gradient(90deg, #fff 1px, transparent 1px); background-size: 40px 40px;"
      ></div>

      <div class="relative max-w-7xl mx-auto px-6 md:px-10 py-16">
        <div class="text-xs uppercase tracking-widest opacity-60 mb-4 font-mono">
          [ what makes me different ]
        </div>
        <h2 class="text-3xl md:text-5xl font-black tracking-tight leading-[1.1] max-w-4xl">
          內訓現場的範例，<br />
          <span class="bg-[var(--color-neub-yellow)] text-black px-2">就是我自己 production 系統的同一段 code。</span>
        </h2>

        <div class="mt-8 grid gap-4 md:grid-cols-3 max-w-5xl">
          <div class="border-l-2 border-white/30 pl-4">
            <div class="text-[10px] uppercase tracking-widest opacity-50 font-mono mb-1">
              today
            </div>
            <div class="text-base font-black leading-tight">
              我每天用 Claude Code、AI Agent 寫 production
            </div>
          </div>
          <div class="border-l-2 border-white/30 pl-4">
            <div class="text-[10px] uppercase tracking-widest opacity-50 font-mono mb-1">
              this month
            </div>
            <div class="text-base font-black leading-tight">
              整理成你團隊一天可上手的內訓
            </div>
          </div>
          <div class="border-l-2 border-[var(--color-neub-yellow)] pl-4">
            <div class="text-[10px] uppercase tracking-widest opacity-70 font-mono mb-1 text-[var(--color-neub-yellow)]">
              your team next week
            </div>
            <div class="text-base font-black leading-tight">
              下課就能在自己工位繼續用
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Production AI 落地經驗 — 真實案例 -->
    <section id="production" data-companion-section="production" class="px-6 md:px-10 py-20 border-b-[3px] border-black bg-white">
      <div class="max-w-7xl mx-auto">
        <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6 mb-10">
          <div>
            <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
              production ai cases · 顧問實作經驗
            </div>
            <h2 class="text-3xl md:text-5xl font-black tracking-tight leading-[1.1]">
              三個<span class="bg-[var(--color-neub-yellow)] px-2">真的上線在跑</span>的<br />
              AI 顧問實作
            </h2>
          </div>
          <p class="text-sm md:text-base leading-relaxed max-w-md opacity-70">
            下面三個是我實際做過、教學現場可直接拆解的案例。
            細節為配合 NDA 已抽象化，但工程實踐是真的。
          </p>
        </div>

        <div class="grid gap-6 md:grid-cols-3">
          {
            productionCases.map((p) => (
              <article
                class="border-2 border-black bg-white p-6 flex flex-col"
                style="box-shadow: 6px 6px 0 #0a0a0a;"
              >
                <div
                  class="inline-block self-start px-2 py-0.5 text-[11px] uppercase tracking-widest font-mono font-black mb-4 border-2 border-black"
                  style={`background: ${p.color};`}
                >
                  {p.domain}
                </div>
                <h3 class="text-xl md:text-2xl font-black leading-tight mb-4">
                  {p.title}
                </h3>
                <p class="text-sm leading-relaxed opacity-80 mb-5 flex-1">
                  {p.body}
                </p>
                <div class="pt-4 border-t-2 border-black">
                  <div class="text-[10px] uppercase tracking-widest opacity-60 font-mono mb-1.5">
                    fit for · 適合你們的場景
                  </div>
                  <div class="text-sm leading-snug font-bold">
                    {p.fitFor}
                  </div>
                </div>
              </article>
            ))
          }
        </div>

        <p class="mt-8 text-sm md:text-base font-mono opacity-70 max-w-3xl leading-relaxed">
          ▸ 看到對應到你們的場景？這些案的選型、踩坑、最後怎麼結，可以在內訓 / 顧問現場直接拆給你工程團隊看。
        </p>
      </div>
    </section>

    <!-- Portfolio — 看得到的作品（直接連到 GitHub） -->
    <section id="portfolio" data-companion-section="portfolio" class="px-6 md:px-10 py-20 border-b-[3px] border-black bg-[#fafaf6]">
      <div class="max-w-7xl mx-auto">
        <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6 mb-10">
          <div>
            <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
              portfolio · 看得到的作品
            </div>
            <h2 class="text-3xl md:text-5xl font-black tracking-tight leading-[1.1]">
              code 都<span class="bg-[var(--color-neub-yellow)] px-2">公開在 GitHub</span>，<br />
              不是 PPT 上的口頭專案
            </h2>
          </div>
          <p class="text-sm md:text-base leading-relaxed max-w-md opacity-70">
            上面 Production 案是顧問實作（受 NDA 保密）。下面這些是公開可看、可 clone、可審視的——
            判斷我這個人寫不寫 code，看這裡最快。
          </p>
        </div>

        <div class="grid gap-6 md:grid-cols-2">
          {
            portfolio.map((p) => (
              <a
                href={p.link}
                target="_blank"
                rel="noopener"
                class="group block border-2 border-black bg-white p-6 transition-transform hover:-translate-x-1 hover:-translate-y-1"
                style="box-shadow: 6px 6px 0 #0a0a0a;"
              >
                <div class="flex items-start justify-between gap-4 mb-4">
                  <div
                    class="inline-block px-2 py-0.5 text-[11px] uppercase tracking-widest font-mono font-black border-2 border-black"
                    style={`background: ${p.color};`}
                  >
                    {p.label}
                  </div>
                  <span class="text-xs font-mono opacity-50 group-hover:opacity-100 transition-opacity uppercase tracking-widest">
                    github →
                  </span>
                </div>

                <h3 class="text-xl md:text-2xl font-black leading-tight mb-1">
                  {p.title}
                </h3>
                <div class="text-xs md:text-sm font-mono opacity-60 mb-4 uppercase tracking-wider">
                  {p.subtitle}
                </div>

                <p class="text-sm leading-relaxed opacity-85 mb-5">
                  {p.body}
                </p>

                <div class="pt-4 border-t-2 border-black/10 flex flex-wrap gap-2">
                  {p.techs.map((t) => (
                    <span class="text-[11px] font-mono px-2 py-0.5 border border-black/30">
                      {t}
                    </span>
                  ))}
                </div>
              </a>
            ))
          }
        </div>

        <p class="mt-8 text-sm md:text-base font-mono opacity-70 max-w-3xl leading-relaxed">
          ▸ 完整作品清單在
          <a href="https://github.com/yanchen184" target="_blank" rel="noopener" class="underline font-black hover:bg-[var(--color-neub-yellow)] px-1">
            github.com/yanchen184
          </a>
          ——產品、教材、玩具專案、實驗 repo 都在。
        </p>
      </div>
    </section>

    <!-- About -->
    <section id="about" data-companion-section="about" class="px-6 md:px-10 py-20 border-b-[3px] border-black bg-white">
      <div class="max-w-7xl mx-auto grid gap-12 lg:grid-cols-[1fr_2fr] items-start">
        <div>
          <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
            about me
          </div>
          <h2 class="text-3xl md:text-4xl font-black tracking-tight leading-tight">
            關於我
          </h2>
          <div class="mt-6 inline-block bg-[var(--color-neub-yellow)] px-3 py-1 text-sm font-bold">
            陳彥彤 · Bob Chen
          </div>

          <div
            class="mt-8 border-2 border-black bg-white p-2 max-w-[260px]"
            style="box-shadow: 6px 6px 0 #0a0a0a;"
          >
            <img
              src={withBase('/images/character/head_01.png')}
              alt="陳彥彤 YC — 來吧"
              width="512"
              height="512"
              class="block w-full h-auto"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>

        <div class="space-y-5 text-base md:text-lg leading-relaxed">
          <p>
            <strong>Java 後端 8 年、AI 工程師 2 年。</strong>
            出身電商核心系統團隊（WMS / MOX / MIX），主責架構設計、跨團隊整合、線上事故處理。
          </p>
          <p>
            近兩年主導 <strong>AMI 照護平台</strong>從零到上線——6 個 repo 的醫療微服務體系，
            涵蓋醫師工作台、民眾端 LINE LIFF、HIS 介接、AI 服務（OCR + 去敏 + Hermes Multi-Agent + RAG）。
            <strong>要過資安、要接 SSO、要評估成本、要監控品質、要長期維運。</strong>
            這些不是 demo——是真的在跑、真的要對 PM 解釋為什麼 token 預算超標的系統。
          </p>
          <p>
            另一條線是<strong>教學</strong>：政府 <strong>職訓局 AI 概論初級班</strong>講師、
            企業內訓 10-50 場（Claude Code、AI Agent、Docker、Kubernetes、Java 高併發），
            教材全部公開在 GitHub，學員下課可以直接 clone 回家繼續看。
          </p>
          <p class="text-base opacity-75 italic border-l-4 border-black pl-4">
            我不是「以前是工程師現在轉講師」——我是<strong>邊做 production 邊教書的工程師</strong>。
            今天卡的 bug，下個月變成內訓現場的真實 case。
          </p>

          <div class="grid grid-cols-2 md:grid-cols-4 gap-3 pt-4">
            {
              [
                { title: 'AI 落地', desc: 'Multi-Agent · RAG · OCR' },
                { title: '後端專精', desc: 'Spring Boot · Java 8 年' },
                { title: '職訓局講師', desc: 'AI 概論初級班' },
                { title: '企業內訓', desc: '10-50 場累積' },
              ].map((i) => (
                <div class="border-2 border-black p-3">
                  <div class="font-black text-sm">{i.title}</div>
                  <div class="text-xs opacity-70 mt-1">{i.desc}</div>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </section>

    <!-- Skills -->
    <section id="skills" data-companion-section="skills" class="px-6 md:px-10 py-20 border-b-[3px] border-black">
      <div class="max-w-7xl mx-auto">
        <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
          tech stack
        </div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight mb-10">
          技術專長
        </h2>

        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
          {
            skills.map((s) => (
              <div
                class="border-2 border-black bg-white overflow-hidden"
                style="box-shadow: 4px 4px 0 #0a0a0a;"
              >
                <div
                  class="px-5 py-3 border-b-2 border-black flex items-center justify-between font-mono"
                  style={`background: ${s.color};`}
                >
                  <span class="text-lg font-black">{s.group}</span>
                  <span class="text-[10px] uppercase tracking-widest opacity-70">
                    {s.label}
                  </span>
                </div>
                <ul class="p-5 space-y-2 text-sm font-bold">
                  {s.items.map((it) => (
                    <li class="flex items-start gap-2">
                      <span class="text-black/40 select-none">▸</span>
                      <span>{it}</span>
                    </li>
                  ))}
                </ul>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Courses -->
    <section id="courses" data-companion-section="courses" class="px-6 md:px-10 py-20 border-b-[3px] border-black bg-white">
      <div class="max-w-7xl mx-auto">
        <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
          who are you · 對應方案
        </div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight mb-3">
          告訴我你是誰，<br class="md:hidden" />我給你對應方案
        </h2>
        <p class="text-sm md:text-base opacity-70 max-w-3xl leading-relaxed mb-10">
          下面三種角色找我的需求最常見。挑一張像你的卡片，直接點下方 CTA 寄信，<strong>主旨已經幫你寫好</strong>。
        </p>

        <div class="grid gap-6 md:grid-cols-3">
          {
            courses.map((c) => (
              <div
                class="relative border-2 border-black bg-white p-6 flex flex-col"
                style="box-shadow: 6px 6px 0 #0a0a0a;"
              >
                <div
                  class="absolute -top-3 -left-3 px-2 py-0.5 text-xs font-black uppercase border-2 border-black font-mono bg-[var(--color-neub-yellow)]"
                >
                  {c.badge}
                </div>
                <div class="text-[11px] uppercase tracking-widest opacity-70 mb-2 font-mono">
                  if you are
                </div>
                <div class="text-base font-black mb-4 leading-tight">
                  {c.persona}
                </div>
                <h3 class="text-xl font-black leading-tight mb-3 border-t-2 border-black pt-4">
                  {c.title}
                </h3>
                <p class="text-sm leading-relaxed opacity-80 mb-4">
                  <strong>你的痛：</strong>{c.problem}
                </p>
                <div class="text-[11px] uppercase tracking-widest opacity-60 mb-2 font-mono">
                  我給你
                </div>
                <ul class="space-y-2 text-sm mb-5 flex-1">
                  {c.deliver.map((d) => (
                    <li class="flex items-start gap-2 leading-relaxed">
                      <span class="text-black/40 select-none">▸</span>
                      <span>{d}</span>
                    </li>
                  ))}
                </ul>
                <div class="border-t-2 border-black pt-3 mt-auto flex items-center justify-between text-xs font-mono">
                  <span class="opacity-70">時長：<strong>{c.duration}</strong></span>
                  <a
                    href={`mailto:bobchen184@gmail.com?subject=${encodeURIComponent(`[${c.persona}] 想聊聊 ${c.title}`)}&body=${encodeURIComponent(`嗨 YC，\n\n我是 ___（公司／團隊）___ 的 ${c.persona}。\n\n• 目前情境：\n• 想解決的問題：\n• 預期時程：\n• 大概人數 / 預算範圍：\n\n謝謝！`)}`}
                    class="font-black uppercase tracking-widest hover:underline"
                  >
                    寄信聊 →
                  </a>
                </div>
              </div>
            ))
          }
        </div>

      </div>
    </section>

    {/* Testimonials 暫時隱藏：等累積真實具名回饋再放回 */}

    <!-- FAQ -->
    <section id="faq" data-companion-section="faq" class="px-6 md:px-10 py-20 border-b-[3px] border-black bg-[#fafaf6]">
      <div class="max-w-7xl mx-auto">
        <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
          frequently asked · 寄信前先看這裡
        </div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight mb-3">
          常見問題
        </h2>
        <p class="text-sm md:text-base opacity-70 max-w-2xl leading-relaxed mb-10">
          下面這些是 HR / 主管最常在第一封信前想知道的事——直接寫在這裡，省你多寄一輪。
        </p>

        <div class="grid gap-4 md:grid-cols-2">
          {
            faqs.map((f, i) => (
              <details
                class="group border-2 border-black bg-white"
                style="box-shadow: 4px 4px 0 #0a0a0a;"
              >
                <summary class="cursor-pointer list-none p-5 flex items-start gap-4 hover:bg-[var(--color-neub-yellow)] transition-colors">
                  <span class="text-xs font-mono font-black opacity-50 mt-0.5 shrink-0">
                    Q{(i + 1).toString().padStart(2, '0')}
                  </span>
                  <span class="font-black text-base leading-snug flex-1">
                    {f.q}
                  </span>
                  <span class="font-mono opacity-50 transition-transform group-open:rotate-90 shrink-0">
                    ›
                  </span>
                </summary>
                <div class="px-5 pb-5 pl-12 text-sm leading-relaxed opacity-85 border-t-2 border-black/10">
                  <p class="pt-4">{f.a}</p>
                </div>
              </details>
            ))
          }
        </div>

        <p class="mt-8 text-sm font-mono opacity-70">
          ▸ 還有其他疑問？直接寄信問就好——
          <a href="mailto:bobchen184@gmail.com" class="underline font-black hover:bg-[var(--color-neub-yellow)] px-1">
            bobchen184@gmail.com
          </a>
        </p>
      </div>
    </section>

    <!-- Contact -->
    <section id="contact" data-companion-section="contact" class="px-6 md:px-10 py-20 border-b-[3px] border-black bg-white">
      <div class="max-w-7xl mx-auto">
        <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
          next step · 下一步
        </div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight mb-3">
          挑你最舒服的下一步
        </h2>
        <p class="text-base md:text-lg leading-relaxed opacity-80 mb-10 max-w-3xl">
          不用先想清楚才能聯繫。挑一個對應你現在狀態的入口，
          <strong>對應的信件主旨已經幫你寫好</strong>，回信節奏通常 1-2 個工作日。
        </p>

        <div class="grid gap-6 md:grid-cols-3">
          <!-- Path 1: 想先聊聊 -->
          <a
            href={`mailto:bobchen184@gmail.com?subject=${encodeURIComponent('[30 分鐘聊聊] 想先了解你的內訓模式')}&body=${encodeURIComponent('嗨 YC，\n\n我是 ___（公司／團隊）___ 的 ___（職稱）___。\n\n我還在評估階段，想先約 30 分鐘聊聊：\n• 我們團隊狀況：\n• 在意的問題（不確定也沒關係）：\n• 方便的時段：\n\n謝謝！')}`}
            class="block border-2 border-black bg-white p-6 transition-transform hover:-translate-x-1 hover:-translate-y-1 group"
            style="box-shadow: 6px 6px 0 #0a0a0a;"
          >
            <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
              path 01 · 探索期
            </div>
            <div class="text-2xl font-black mb-2 leading-tight group-hover:bg-[var(--color-neub-yellow)] inline-block px-1">
              還在想要不要做？
            </div>
            <p class="text-sm opacity-80 leading-relaxed mb-4">
              30 分鐘免費通話。先聊你們的痛點與現況，我直接告訴你<strong>需不需要找講師</strong>——
              如果不需要，我會跟你說。
            </p>
            <div class="text-xs uppercase tracking-widest font-black font-mono">
              寄信約聊 →
            </div>
          </a>

          <!-- Path 2: 已經有需求 -->
          <a
            href={`mailto:bobchen184@gmail.com?subject=${encodeURIComponent('[已有需求] AI 內訓 / 顧問 詢價')}&body=${encodeURIComponent('嗨 YC，\n\n我是 ___（公司）___ 的 ___（職稱）___。\n\n我們的需求：\n• 想做的類型（內訓 / 顧問 / 兩者）：\n• 對象（工程師 / PM / 主管）：\n• 大概人數：\n• 預期時程：\n• 已知的場景或痛點：\n\n謝謝！')}`}
            class="block border-2 border-black bg-[var(--color-neub-yellow)] p-6 transition-transform hover:-translate-x-1 hover:-translate-y-1 group"
            style="box-shadow: 6px 6px 0 #0a0a0a;"
          >
            <div class="text-xs uppercase tracking-widest opacity-70 mb-3 font-mono">
              path 02 · 評估期 ★ 推薦
            </div>
            <div class="text-2xl font-black mb-2 leading-tight">
              已經有需求了？
            </div>
            <p class="text-sm leading-relaxed mb-4 opacity-90">
              直接寄信，<strong>主旨與問題清單都幫你寫好</strong>——填完寄出，
              我這邊 1-2 個工作日給你初步建議與報價。
            </p>
            <div class="text-xs uppercase tracking-widest font-black font-mono">
              直接寄信 →
            </div>
          </a>

          <!-- Path 3: 想看樣本 -->
          <div
            class="border-2 border-black bg-white p-6"
            style="box-shadow: 6px 6px 0 #0a0a0a;"
          >
            <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
              path 03 · 觀察期
            </div>
            <div class="text-2xl font-black mb-2 leading-tight">
              想先看作品？
            </div>
            <p class="text-sm opacity-80 leading-relaxed mb-4">
              不急著聊。
              先看技術筆記與 GitHub 判斷我這個人合不合你們團隊的口味。
            </p>
            <div class="space-y-2">
              <a
                href={withBase('/blog')}
                class="flex items-center justify-between border-2 border-black px-3 py-2 text-sm hover:bg-[var(--color-neub-yellow)] transition-colors"
              >
                <span class="font-black">技術部落格</span>
                <span>→</span>
              </a>
              <a
                href="https://github.com/yanchen184"
                target="_blank"
                rel="noopener"
                class="flex items-center justify-between border-2 border-black px-3 py-2 text-sm hover:bg-[var(--color-neub-yellow)] transition-colors"
              >
                <span class="font-black">GitHub @yanchen184</span>
                <span>→</span>
              </a>
            </div>
          </div>
        </div>

        <!-- 直接資訊 -->
        <div class="mt-10 flex flex-wrap items-baseline gap-x-6 gap-y-2 text-sm font-mono opacity-80 border-t-2 border-black pt-6">
          <span class="font-black uppercase tracking-widest">direct contact</span>
          <a href="mailto:bobchen184@gmail.com" class="hover:bg-[var(--color-neub-yellow)] px-1">bobchen184@gmail.com</a>
          <span class="opacity-40">·</span>
          <a href="https://github.com/yanchen184" target="_blank" rel="noopener" class="hover:bg-[var(--color-neub-yellow)] px-1">github.com/yanchen184</a>
          <span class="opacity-40">·</span>
          <span>地點：台灣（線上 / 北中南實體皆可）</span>
        </div>
      </div>
    </section>

    <!-- Latest Posts — 給「Path 03 觀察期」讀者的探索區 -->
    {
      latest.length > 0 && (
        <section data-companion-section="latest" class="px-6 md:px-10 py-16 border-b-[3px] border-black bg-[#fafaf6]">
          <div class="max-w-7xl mx-auto">
            <div class="flex flex-col sm:flex-row sm:items-baseline sm:justify-between gap-3 mb-8">
              <div>
                <div class="text-xs uppercase tracking-widest opacity-60 mb-2 font-mono">
                  latest posts · 還想多看一點？
                </div>
                <h2 class="text-2xl md:text-3xl font-black tracking-tight">
                  最新技術筆記
                </h2>
              </div>
              <a
                href={withBase('/blog')}
                class="self-start sm:self-auto text-xs uppercase tracking-widest underline font-mono hover:bg-[var(--color-neub-yellow)] px-1"
              >
                all posts →
              </a>
            </div>

            <div
              class:list={[
                'grid gap-0 border-t-2 border-black bg-white',
                latest.length >= 3 ? 'md:grid-cols-3' : 'md:grid-cols-2',
              ]}
            >
              {latest.map((p, i) => (
                <a
                  href={withBase(`/blog/${p.slug}`)}
                  class:list={[
                    'block p-5 border-b-2 border-black hover:bg-[var(--color-neub-yellow)] transition-colors',
                    i < latest.length - 1 ? 'md:border-r-2' : '',
                  ]}
                >
                  <div class="text-[11px] uppercase tracking-widest opacity-60 mb-2 font-mono">
                    [{p.category}] · {formatDate(p.publishDate)}
                  </div>
                  <h3 class="text-base font-black leading-tight mb-2">{p.title}</h3>
                  <p class="text-sm opacity-70 line-clamp-2">{p.excerpt}</p>
                </a>
              ))}
            </div>
          </div>
        </section>
      )
    }

    <!-- Message Board (Firebase island, lazy) -->
    <MessageBoard client:visible />
  </main>

  <FooterNeub />

  <!-- Character video companion (floating bottom-right, scroll-aware) -->
  <CharacterCompanion client:load />
</RootLayout>
