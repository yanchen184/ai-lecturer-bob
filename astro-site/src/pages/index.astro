---
/**
 * / — 形象首頁（Hero → 關於 → 技能棧 → 課程 → 最新文章 → 聯繫）
 * 保留 Neub 風格底但移除 /// // 裝飾前綴。
 */
import RootLayout from '../layouts/RootLayout.astro';
import NavbarNeub from '../components/NavbarNeub.astro';
import FooterNeub from '../components/FooterNeub.astro';
import { getAllPublishedPosts } from '../lib/firestore';
import { withBase } from '../lib/url';

const posts = await getAllPublishedPosts();
const latest = posts.slice(0, 3);
const formatDate = (d: string) => d.replaceAll('-', '/');

const stats = [
  { number: '5-6 年', label: 'Java 後端開發經驗' },
  { number: '10-50 場', label: '企業授課經歷' },
  { number: 'AZ-900', label: 'Azure 雲端認證' },
  { number: '98%', label: '學員滿意度' },
];

const skills = [
  { group: '後端', items: ['Spring Boot 4', 'Java 21', 'Maven', 'JPA / Hibernate'] },
  { group: '資料', items: ['MySQL', 'MSSQL', 'Redis', 'RabbitMQ'] },
  { group: '前端', items: ['React 19', 'TypeScript', 'Vite', 'Tailwind CSS'] },
  { group: 'DevOps', items: ['Docker', 'GitHub Actions', 'Firebase', 'Azure AZ-900'] },
];

const courses = [
  {
    title: '企業內訓 · Spring Boot 全棧',
    desc: '從專案結構、分層架構、Repository/Service 設計到 JPA、快取、Redis 整合。用實際電商案例帶學員跑一輪。',
    tags: ['Spring Boot', 'JPA', 'Redis'],
  },
  {
    title: '一對一技術諮詢',
    desc: '系統架構、效能調校、AI 導入、舊系統遷移（Grails → Spring Boot）。針對你實際的專案給出可落地的解法。',
    tags: ['Architecture', 'Performance', 'AI 導入'],
  },
  {
    title: 'React 前端實戰',
    desc: '以電商、後台應用為例，從元件設計、狀態管理、API 整合到測試一次帶完，不走 Hello World 教材。',
    tags: ['React 19', 'TypeScript', 'Testing'],
  },
];
---

<RootLayout
  title="AI 講師陳彥彤YC — 後端工程師 · Spring Boot / React 企業內訓"
  description="陳彥彤 Bob Chen — 資深後端工程師 · AI 講師。5-6 年電商核心系統開發經驗，10-50 場企業授課經歷。提供 Spring Boot、React、MySQL、Redis 企業內訓與技術諮詢。"
  canonical="/"
>
  <NavbarNeub />

  <main class="font-sans text-[#0a0a0a]">
    <!-- Hero -->
    <section class="px-6 md:px-10 py-20 md:py-28 border-b-[3px] border-black">
      <div class="max-w-7xl mx-auto">
        <div class="text-xs uppercase tracking-widest opacity-60 mb-4 font-mono">
          ai lecturer · senior backend engineer
        </div>
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-black leading-[0.95] tracking-tight">
          你好，我是<br />
          <span class="bg-[var(--color-neub-yellow)] px-3 inline-block mt-2">程式講師陳彥彤</span>
        </h1>
        <p class="mt-8 text-lg md:text-xl max-w-3xl leading-relaxed opacity-80">
          資深後端工程師 · AI 講師。<br />
          Spring Boot / React / MySQL / Redis 實戰派。企業內訓、技術寫作、系統架構諮詢。
        </p>

        <div class="mt-10 flex flex-wrap gap-3">
          <a
            href="#courses"
            class="px-6 py-3 bg-black text-white text-sm uppercase tracking-widest hover:bg-[var(--color-neub-yellow)] hover:text-black transition-colors font-mono"
          >
            預約課程諮詢 →
          </a>
          <a
            href={withBase('/blog')}
            class="px-6 py-3 border-2 border-black text-sm uppercase tracking-widest hover:bg-[var(--color-neub-yellow)] transition-colors font-mono"
          >
            瀏覽部落格
          </a>
        </div>

        <!-- Stats -->
        <div class="mt-16 grid grid-cols-2 md:grid-cols-4 gap-4">
          {
            stats.map((s) => (
              <div class="border-2 border-black bg-white p-5">
                <div class="text-2xl md:text-3xl font-black tracking-tight">{s.number}</div>
                <div class="text-xs opacity-70 mt-1">{s.label}</div>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- About -->
    <section id="about" class="px-6 md:px-10 py-20 border-b-[3px] border-black bg-white">
      <div class="max-w-7xl mx-auto grid gap-12 lg:grid-cols-[1fr_2fr] items-start">
        <div>
          <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
            about me
          </div>
          <h2 class="text-3xl md:text-4xl font-black tracking-tight leading-tight">
            關於我
          </h2>
          <div class="mt-6 inline-block bg-[var(--color-neub-yellow)] px-3 py-1 text-sm font-bold">
            陳彥彤 · Bob Chen
          </div>
        </div>

        <div class="space-y-5 text-base md:text-lg leading-relaxed">
          <p>
            大家好，我是<strong>陳彥彤</strong>，一位<strong>資深後端工程師</strong>兼<strong>程式講師</strong>。
            擁有 5-6 年 Java 後端開發經驗，專精於企業級系統架構與高併發解決方案。
          </p>
          <p>
            曾任職於電商核心系統團隊，主責智能倉儲系統（WMS）、訂單管理系統（MOX）、貨品管理系統（MIX）的開發與維護。
            系統每日處理上萬筆 API 請求，平均延遲維持低於 200ms。
          </p>
          <p>
            技術專長包含 Spring Boot、MySQL、Redis、RabbitMQ 等後端技術，同時具備 React 前端開發能力。
            現在將這些年累積的實戰經驗轉化為系統化的教學內容，幫助想成為後端工程師或提升技術能力的學員，用最有效率的方式掌握程式開發技能。
          </p>

          <div class="grid grid-cols-2 md:grid-cols-4 gap-3 pt-4">
            {
              [
                { title: '後端專精', desc: 'Spring Boot 企業級' },
                { title: '電商經驗', desc: '核心系統實戰' },
                { title: '實戰導向', desc: '即學即用' },
                { title: '全端能力', desc: '前後端整合' },
              ].map((i) => (
                <div class="border-2 border-black p-3">
                  <div class="font-black text-sm">{i.title}</div>
                  <div class="text-xs opacity-70 mt-1">{i.desc}</div>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </section>

    <!-- Skills -->
    <section id="skills" class="px-6 md:px-10 py-20 border-b-[3px] border-black">
      <div class="max-w-7xl mx-auto">
        <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
          tech stack
        </div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight mb-10">
          技術專長
        </h2>

        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
          {
            skills.map((s) => (
              <div class="border-2 border-black bg-white p-6">
                <div class="text-xs uppercase tracking-widest opacity-70 font-mono mb-3">
                  {s.group}
                </div>
                <ul class="space-y-2 text-sm font-bold">
                  {s.items.map((it) => (
                    <li>· {it}</li>
                  ))}
                </ul>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Courses -->
    <section id="courses" class="px-6 md:px-10 py-20 border-b-[3px] border-black bg-white">
      <div class="max-w-7xl mx-auto">
        <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
          services
        </div>
        <h2 class="text-3xl md:text-4xl font-black tracking-tight mb-10">
          課程與服務
        </h2>

        <div class="grid gap-6 md:grid-cols-3">
          {
            courses.map((c) => (
              <div class="border-2 border-black p-6 hover:bg-[var(--color-neub-yellow)] transition-colors">
                <h3 class="text-lg font-black leading-tight mb-3">{c.title}</h3>
                <p class="text-sm leading-relaxed opacity-80 mb-4">{c.desc}</p>
                <div class="flex flex-wrap gap-1.5">
                  {c.tags.map((t) => (
                    <span class="text-xs border border-black px-1.5 py-0.5 font-mono">
                      #{t}
                    </span>
                  ))}
                </div>
              </div>
            ))
          }
        </div>

        <div class="mt-10 text-center">
          <a
            href="#contact"
            class="inline-block px-6 py-3 bg-black text-white text-sm uppercase tracking-widest hover:bg-[var(--color-neub-yellow)] hover:text-black transition-colors font-mono"
          >
            立即聯繫 →
          </a>
        </div>
      </div>
    </section>

    <!-- Latest Posts -->
    {
      latest.length > 0 && (
        <section class="px-6 md:px-10 py-20 border-b-[3px] border-black">
          <div class="max-w-7xl mx-auto">
            <div class="flex items-baseline justify-between mb-10">
              <div>
                <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
                  latest posts
                </div>
                <h2 class="text-3xl md:text-4xl font-black tracking-tight">
                  最新文章
                </h2>
              </div>
              <a
                href={withBase('/blog')}
                class="text-xs uppercase tracking-widest underline font-mono hover:bg-[var(--color-neub-yellow)] px-1"
              >
                all posts →
              </a>
            </div>

            <div class="grid gap-0 md:grid-cols-3 border-t-2 border-black">
              {latest.map((p, i) => (
                <a
                  href={withBase(`/blog/${p.slug}`)}
                  class:list={[
                    'block p-6 border-b-2 border-black hover:bg-[var(--color-neub-yellow)] transition-colors',
                    i < 2 ? 'md:border-r-2' : '',
                  ]}
                >
                  <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
                    [{p.category}] · {formatDate(p.publishDate)}
                  </div>
                  <h3 class="text-lg font-black leading-tight mb-2">{p.title}</h3>
                  <p class="text-sm opacity-70 line-clamp-2">{p.excerpt}</p>
                </a>
              ))}
            </div>
          </div>
        </section>
      )
    }

    <!-- Contact -->
    <section id="contact" class="px-6 md:px-10 py-20 bg-white">
      <div class="max-w-7xl mx-auto grid gap-10 md:grid-cols-2">
        <div>
          <div class="text-xs uppercase tracking-widest opacity-60 mb-3 font-mono">
            contact
          </div>
          <h2 class="text-3xl md:text-4xl font-black tracking-tight mb-6">
            開始合作
          </h2>
          <p class="text-base leading-relaxed opacity-80">
            企業內訓、技術諮詢、系統架構顧問 —— 歡迎透過 Email 或 GitHub 聯繫。
            一對一諮詢與團隊培訓皆可客製化內容，先聊需求再報價。
          </p>
        </div>

        <div class="space-y-4">
          <a
            href="mailto:bobchen184@gmail.com"
            class="flex items-center justify-between border-2 border-black p-5 hover:bg-[var(--color-neub-yellow)] transition-colors"
          >
            <div>
              <div class="text-xs uppercase tracking-widest opacity-60 font-mono mb-1">
                email
              </div>
              <div class="font-black">bobchen184@gmail.com</div>
            </div>
            <span class="text-xl">→</span>
          </a>

          <a
            href="https://github.com/yanchen184"
            target="_blank"
            rel="noopener"
            class="flex items-center justify-between border-2 border-black p-5 hover:bg-[var(--color-neub-yellow)] transition-colors"
          >
            <div>
              <div class="text-xs uppercase tracking-widest opacity-60 font-mono mb-1">
                github
              </div>
              <div class="font-black">yanchen184</div>
            </div>
            <span class="text-xl">→</span>
          </a>

          <a
            href={withBase('/blog')}
            class="flex items-center justify-between border-2 border-black p-5 hover:bg-[var(--color-neub-yellow)] transition-colors"
          >
            <div>
              <div class="text-xs uppercase tracking-widest opacity-60 font-mono mb-1">
                blog
              </div>
              <div class="font-black">技術文章與實戰筆記</div>
            </div>
            <span class="text-xl">→</span>
          </a>
        </div>
      </div>
    </section>
  </main>

  <FooterNeub />
</RootLayout>
